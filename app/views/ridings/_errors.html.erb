<div style="color: red">
  <h2><%= pluralize(riding.errors.count, "error") %> prohibited this from being saved:</h2>

  <ul>
    <% riding.errors.each do |error| %>
      <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>
